# =====================================================
# Core Framework
# =====================================================
fastapi==0.110.0                 # FastAPI framework
uvicorn[standard]==0.27.1        # ASGI server with reload, logging, uvloop


# =====================================================
# Database & ORM
# =====================================================
SQLAlchemy==2.0.25               # ORM (SQLAlchemy 2.x style)
psycopg2-binary==2.9.9           # PostgreSQL driver (production)
alembic==1.13.1                  # Database migrations


# =====================================================
# SQLite (Development / Local)
# =====================================================
aiosqlite==0.19.0                # Async SQLite support


# =====================================================
# Authentication & Security
# =====================================================
authlib==1.3.0                   # OAuth (Google login)
python-jose[cryptography]==3.3.0 # JWT encode/decode
passlib[bcrypt]==1.7.4           # Password hashing (future-ready)
itsdangerous==2.1.2              # Session signing (Starlette middleware)


# =====================================================
# HTTP & OAuth Utilities
# =====================================================
httpx==0.26.0                    # Async HTTP client (OAuth callbacks)
python-multipart==0.0.9          # Form-data handling (OAuth)


# =====================================================
# Environment & Configuration
# =====================================================
python-dotenv==1.0.1             # Load .env files
pydantic-settings==2.1.0         # Environment-based settings


# =====================================================
# Data Validation & Serialization
# =====================================================
pydantic==2.6.1                  # Data validation (FastAPI compatible)
email-validator==2.1.0.post1     # Email validation for auth
orjson==3.9.15                   # Fast JSON serialization (performance)


# =====================================================
# Logging
# =====================================================
loguru==0.7.2                    # Structured, easy logging


# =====================================================
# Testing (Recommended)
# =====================================================
pytest==8.0.0
pytest-asyncio==0.23.5
